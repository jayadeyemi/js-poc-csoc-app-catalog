apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Placeholder — add kube-prometheus-stack HelmChart or Prometheus/Grafana manifests here.
# This directory is deployed when csoc.js2.org/observability=enabled on a spoke cluster.
resources: []
