apiVersion: v1
kind: Service
metadata:
  name: hello-app
  namespace: hello-app
  labels:
    app: hello-app
    csoc.js2.org/managed: "true"
spec:
  selector:
    app: hello-app
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
