apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Placeholder — add Falco or OPA Gatekeeper resources here.
# This directory is deployed when csoc.js2.org/security=enabled on a spoke cluster.
resources: []
